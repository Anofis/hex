#ifndef __SHORT_H__
#define __SHORT_H__

#include <stdlib.h>
#include <stdio.h>
#include <assert.h>

int present (Node* t, int j, Node n);

int point (Node n, Node g);

Graph regroup (Graph g, Node n);

#endif